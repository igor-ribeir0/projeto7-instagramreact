import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.querySelector(".root"));

//OBS: está tendo que dar 2 clicks no like pra ele começar a funcionar. 
//Deve ser alguma checagem do React que faz com que seja necessário clicar 2 vezes.